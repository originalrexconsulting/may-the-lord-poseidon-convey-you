# Setup on Windows

Run it under WSL 2 as Linux. A native Windows port is not done: Python on Windows has
no curses without the `windows-curses` package, and mpv's control socket would have
to become a named pipe.

Guide to follow: WSL 2 with Ubuntu, the Linux packages, and audio through WSLg. Not
written yet.
