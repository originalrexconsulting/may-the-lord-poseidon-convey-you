# the whole audio tree: the music itself (31 GB+), the playlist snapshots,
# and the .nobackup tagfile that keeps it out of the restic repos
dead/
__pycache__/
*.pyc
*.part
# house-specific notes and the owner's manuals stay off GitHub
PRIVATE.md
manuals/
build/
dist/
